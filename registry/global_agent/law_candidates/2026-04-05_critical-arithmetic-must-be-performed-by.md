# LAW CANDIDATE — 2026-04-05

**Rule:** Critical arithmetic must be performed by deterministic Python, not by LLMs.

Agents must never perform critical arithmetic calculations; these operations must be strictly offloaded to deterministic Python boundaries to ensure actuarial accuracy and prevent any form of LLM hallucination or error in numerical results.

**Tags:** LLM limitations, rule, AI safety, deterministic calculation, architecture
