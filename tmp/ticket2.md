## Context
We need a top-level deterministic catalog (`index.md`) mapping the topology of the registry. This gives agents a zero-hallucination "Map" to navigate before firing `semantic_search`.

## DoD
- [ ] Create `registry/index.md`.
- [ ] Update documentation/macros commanding agents to update `index.md` when creating new structural nodes or sub-directories.
