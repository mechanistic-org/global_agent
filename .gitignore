node_modules/
.venv/
venv/
__pycache__/
.env
.DS_Store
.claude/*
!.claude/commands/
!.claude/commands/**

# Logs and Outputs
*.log
pipeline_output.json

# AST Patcher: ephemeral temp payloads — never commit
scripts/.tmp/
.agent/skills/ast_patcher/node_modules/

gcp-oauth.keys.json

.gdrive_tokens.json

# ChromaDB - binary SQLite, never commit (causes git push hangs)
registry/.chroma_db/
.chroma_db/

# Temp download artifacts
download.txt

# Epic 67 Watchdog Dropzones
inbox/
archive/
